<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"><!-- Untuk Firebase dan membuka WhatsApp -->
<uses-permission android:name="android.permission.INTERNET" />

<!-- Notifikasi Android 13+ -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!-- Membaca kontak untuk tombol KONTAK -->
<uses-permission android:name="android.permission.READ_CONTACTS" />

<!-- Menjadwalkan reminder -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

<application
    android:allowBackup="false"
    android:label="RR MOTOR REMINDER"
    android:supportsRtl="true"
    android:theme="@style/Theme.RRMotorReminder">

    <activity
        android:name=".MainActivity"
        android:exported="true">

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

    </activity>

    <receiver
        android:name=".ReminderReceiver"
        android:exported="false" />

</application>

</manifest>
